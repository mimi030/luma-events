# This workflow will install Python dependencies, run tests and lint with a variety of Python versions
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-python

name: Scrape Luma Events

on:
  schedule:
    - cron: '0 0 1 * *'
  workflow_dispatch:

jobs:
  scrape:

    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11"]

    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}

    - name: Install dependencies
      run: |
        pip install requests beautifulsoup4
      
    - name: Scrape events
      run: |
        cd FeaturesTesting/fetch_luma_past_events/public_page_scraping_soup
        python scrape_luma_events.py
      
    - name: Commit and push changes
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add FeaturesTesting/fetch_luma_past_events/public_page_scraping_soup/data/events.json
        git diff --staged --quiet || git commit -m "Update MLTO events data"
        git push
